#!/usr/bin/env ruby

rails_root = ENV['RAILS_ROOT'] || '../'

APP_PATH = File.expand_path("#{rails_root}/config/application", __dir__)
require_relative "#{rails_root}/config/boot"

require 'trabox/command'

command = ARGV.shift

Trabox::Command.invoke command, ARGV

# vim:ft=ruby:
